package Medium;

public class ZigzagConversion {
    public String convert(String s, int numRows){
        int[] stringBuilders = new int[numRows];
        for(int i = 0; i < numRows; i++){
            String str = "s" + i;
//            stringBuilders[i] = new StringBuilder(str);
        }
        return s;
    }
}
